extends ../layouts/base.pug
include ../components/button.pug
include ../components/urgency.pug

block content
    h1(style="font-weight: normal; margin-bottom: 20px;")= `Дорогой(ая) ${name},`
    p(style="margin-bottom: 15px;") Мы получили запрос на сброс пароля для вашего аккаунта на нашем сайте. Чтобы создать новый пароль, нажмите на кнопку ниже:

    +button(resetUrl, '🔑 Сбросить пароль')
    +urgency()

    p(style="font-size: 14px; color: #999; margin-top: 25px;") Если это были не вы, просто проигнорируйте это письмо — ваш текущий пароль останется без изменений 🤍